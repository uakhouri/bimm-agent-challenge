#!/usr/bin/env node

/**
 * Agent CLI entry point.
 *
 * Usage: npm run agent -- --spec <path> --output <path>
 *
 * This is a stub. The full orchestrator lives in graph.ts and will be
 * wired up in subsequent commits.
 */

function main(): void {
  console.log("[bimm-agent] CLI stub — implementation in progress");
  console.log("[bimm-agent] Pass --spec <path> --output <path> when ready");
}

main();
